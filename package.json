{
  "name": "harness-bootstrap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "audit:adoption": "tsx scripts/audit-adoption.ts",
    "check:harness-state": "tsx scripts/check-harness-state.ts",
    "check:release": "tsx scripts/check-harness-release.ts",
    "check:review-binding": "tsx scripts/check-review-binding.ts",
    "check:targets": "tsx scripts/check-target-inventory.ts",
    "review:digest": "tsx scripts/review-digest.ts",
    "check": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "analyze": "fallow",
    "analyze:changes": "fallow audit",
    "metrics:gate": "tsx scripts/record-gate.ts",
    "metrics:agent": "tsx scripts/record-agent-run.ts",
    "metrics:summary": "tsx scripts/summarize-metrics.ts",
    "test:product": "tsx --test tests/*.test.ts",
    "test:harness:docs": "tsx --test tests/harness/adoption-guidance.test.ts tests/harness/harness-release.test.ts tests/harness/impact-analysis.test.ts tests/harness/local-verification.test.ts tests/harness/repair-loop.test.ts tests/harness/verification-loop.test.ts",
    "test:harness:metrics": "tsx --test tests/metrics/*.test.ts",
    "test:harness": "tsx --test tests/harness/*.test.ts",
    "test": "pnpm run test:product && pnpm run test:harness",
    "feedback": "pnpm run check:harness-state && pnpm run check:release && pnpm run check:review-binding && pnpm run check:targets && pnpm run check && pnpm run lint && pnpm run analyze:changes && pnpm run test:product",
    "verify:docs": "pnpm run check:harness-state && pnpm run check:release && pnpm run check:review-binding && pnpm run check:targets && pnpm run test:harness:docs",
    "verify:local": "tsx scripts/local-verification.ts",
    "impact": "tsx scripts/analyze-impact.ts",
    "verify": "pnpm run feedback && pnpm run test:harness",
    "start": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.0.0",
    "eslint": "^10.9.1",
    "fallow": "^3.19.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.68.0"
  }
}
